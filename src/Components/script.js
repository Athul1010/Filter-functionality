// // Step 1: Select the parent element
// const container = document.getElementById('container');

// // Step 2: Create a new paragraph element
// const newParagraph = document.createElement('p');

// // Step 3: Set the content of the new paragraph
// newParagraph.innerText = 'This is a new paragraph added to the container.';

// // Step 4: Append the new paragraph to the container
// container.appendChild(newParagraph);


const first = document.getElementById('container');

const newParagraph = document.createElement('p');

newParagraph.innerText = "New paragraph is added"

first.appendChild(newParagraph);


